#include <iostream>
#include <memory>
using namespace std ;

class queue {
public :
    void initQueue(shared_ptr<struct node>) ;
private :
    int val ;
    shared_ptr<struct node> next ;
    shared_ptr<shared_ptr<struct node>> tail, head ;
} ;

void queue :: initQueue(shared_ptr<struct node> node) {
    
}

int main() {
    return 0;
}

